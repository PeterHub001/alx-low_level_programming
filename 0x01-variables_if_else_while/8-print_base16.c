#include <stdio.h>
/**
 * main - Entry
 * Return: Aways 0
 */
int main(void)
{
	char c;

	int d;

	c = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(c <= 'f') {
			putcher(c);
			c++;
		}
	putchar('\n');
	return (0);
}
